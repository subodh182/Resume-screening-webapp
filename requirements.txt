flask>=2.3.0
werkzeug>=2.3.0

# MongoDB (optional - install if you have MongoDB running)
pymongo>=4.6.0

# Better PDF/DOCX parsing (optional but recommended)
pdfplumber>=0.10.0
python-docx>=1.1.0

# Install all at once:
# pip install flask werkzeug pymongo pdfplumber python-docx
